name: auto3D
channels:
  - rmg
  - conda-forge
  - rdkit
  - pytorch
  - defaults
dependencies:
  - python>=3.7
  - numpy
  # - pytorch>=2.1.0 # commented out as pytorch should be installed via pip
  - rdkit[version='>=2020.09.01']
  - pandas
